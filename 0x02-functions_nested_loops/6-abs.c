#include "main.h"
/**
 * _abs - print absolute value
 * @b: integer to be printed
 * Return: On success return 1 fail return 0
 */
int _abs(int b)
{
	if (b < 0)
	{
		b = b * -1;
		return (b);
	}
	else if (b > 0)
	{
		return (b);
	}
	else
	{
		return (b);
	}
}
