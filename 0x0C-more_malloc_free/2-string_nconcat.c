#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**

int a, b;

	char *p;

	p = dest;

	for (a = 0; p[a] != '\0'; a++)
	{
	}
	for (b = 0; b < n; b++)
	{
		*(p + a + b) = *(src + b);
	}
	b = b + 1;
	*(p + a + b) = '\0';
