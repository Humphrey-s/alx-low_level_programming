#include "main.h"
/**
 * _strcmp - compares strings
 * @s1: first string
 * @s2: second string
 * Return: (Success)
 */
int _strcmp(char *s1, char *s2)
{
	int a, b;

	for (a = 0; s1[a] != '\0'; a++)
	{
	}
	for (b = 0; s2[b] != '\0'; b++)
	{
	}
	return (a - b);
}
